
const oldState = {};

const DirectMessagesReducer = (state = oldState, action)=>{

  // switch (action.type) {
  //   case expression:
  //
  //     break;
  //   default:
  //
  // }
};

export default DirectMessageReducer;
